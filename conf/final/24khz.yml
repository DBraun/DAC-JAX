$include:
  - conf/base.yml

DAC.sample_rate: 24000

DAC.encoder_rates: [2, 4, 5, 8]

DAC.decoder_rates: [8, 5, 4, 2]

DAC.num_codebooks: 32

DAC.quantizer_dropout: 0.5

Discriminator.sample_rate: 24000

train.num_iterations: 400000